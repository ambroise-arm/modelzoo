name: Docker push on master

on: push

jobs:
  job:
    runs-on: ubuntu-latest
    steps:
      - name: Install docker
        run: sudo apt-get install docker

      - name: Test docker
        run: docker version
